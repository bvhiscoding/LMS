(() => {
  const $=id=>document.getElementById(id),id=Number(new URLSearchParams(location.search).get('id')||1),programs=window.LMSStore.all('training-programs',[]);let program=programs.find(item=>Number(item.id)===id)||programs[0];if(!program){$('programPanel').innerHTML='<p>Không tìm thấy chương trình.</p>';return}const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));const toast=message=>{const node=$('programToast');node.textContent=message;node.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>node.hidden=true,2200)};
  const topicSamples=[['Định hướng và văn hóa doanh nghiệp','2 giờ','Bắt buộc'],['Quy trình, quy định nội bộ','4 giờ','Bắt buộc'],['An toàn và bảo mật thông tin','3 giờ','Bắt buộc'],['Kỹ năng làm việc cơ bản','6 giờ','Tự chọn']];
  function header(){$('programTitle').textContent=program.name;$('programName').textContent=program.name;$('programDescription').textContent=program.description;$('programGroup').textContent=program.group;$('programStatus').textContent=program.status}
  function render(tab='overview'){const panel=$('programPanel');if(tab==='overview')panel.innerHTML=`<div class="app-dialog-summary"><span><b>${program.contents}</b>chủ đề</span><span><b>15 giờ</b>thời lượng</span><span><b>4</b>lớp triển khai</span></div><h2>Mục tiêu chương trình</h2><p>Chuẩn hóa kiến thức, kỹ năng và quy trình làm việc cho nhóm học viên mục tiêu; bảo đảm học viên hoàn thành đầy đủ nội dung bắt buộc và bài đánh giá cuối chương trình.</p><h2 style="margin-top:20px">Đối tượng và điều kiện hoàn thành</h2><ul><li>Đối tượng: cán bộ, nhân viên thuộc đơn vị áp dụng.</li><li>Hoàn thành 100% chủ đề bắt buộc.</li><li>Điểm đánh giá cuối chương trình tối thiểu 70%.</li><li>Chứng chỉ có hiệu lực 24 tháng.</li></ul>`;if(tab==='topics')panel.innerHTML=`<div class="topic-content-head"><h2>Cấu trúc chủ đề</h2><span>${topicSamples.length} chủ đề · 15 giờ</span></div><div class="topic-table-wrap"><table class="topic-table"><thead><tr><th>Thứ tự</th><th>Chủ đề</th><th>Thời lượng</th><th>Yêu cầu</th><th>Trạng thái</th></tr></thead><tbody>${topicSamples.map((row,index)=>`<tr><td>${index+1}</td><td><b>${row[0]}</b></td><td>${row[1]}</td><td>${row[2]}</td><td><span class="topic-status active">Đang sử dụng</span></td></tr>`).join('')}</tbody></table></div>`;if(tab==='classes')panel.innerHTML='<div class="topic-table-wrap"><table class="topic-table"><thead><tr><th>Mã lớp</th><th>Tên lớp</th><th>Thời gian</th><th>Học viên</th><th>Trạng thái</th></tr></thead><tbody><tr><td>HN-2026-01</td><td>Đào tạo hội nhập đợt 1</td><td>01/08 - 15/08/2026</td><td>42 / 50</td><td><span class="topic-status active">Đang diễn ra</span></td></tr><tr><td>HN-2026-02</td><td>Đào tạo hội nhập đợt 2</td><td>01/09 - 15/09/2026</td><td>35 / 50</td><td><span class="topic-status">Sắp diễn ra</span></td></tr></tbody></table></div>';if(tab==='history')panel.innerHTML=`<ol><li><b>Phiên bản 1.2</b> · Cập nhật điều kiện hoàn thành · 15/05/2024</li><li><b>Phiên bản 1.1</b> · Thêm chủ đề An toàn thông tin · 02/05/2024</li><li><b>Phiên bản 1.0</b> · Khởi tạo chương trình · ${esc(program.created)}</li></ol>`}
  document.querySelector('.topic-detail-tabs').addEventListener('click',event=>{const button=event.target.closest('[data-program-tab]');if(!button)return;document.querySelectorAll('[data-program-tab]').forEach(item=>item.classList.toggle('active',item===button));render(button.dataset.programTab)});$('backPrograms').addEventListener('click',()=>location.href='chu-de-chuong-trinh.html?tab=programs');$('editProgram').addEventListener('click',async()=>{const accepted=await window.appDialog({title:'Chỉnh sửa chương trình',html:`<label class="field">Tên chương trình<input id="editProgramName" value="${esc(program.name)}"></label><label class="field">Phân nhóm<input id="editProgramGroup" value="${esc(program.group)}"></label><label class="field">Mô tả<textarea id="editProgramDescription">${esc(program.description)}</textarea></label><label class="field">Trạng thái<select id="editProgramStatus"><option>Hoạt động</option><option>Ngừng hoạt động</option></select></label>`,confirmText:'Lưu thay đổi',cancelText:'Hủy'});if(!accepted)return;program.name=$('editProgramName').value.trim();program.group=$('editProgramGroup').value.trim();program.description=$('editProgramDescription').value.trim();program.status=$('editProgramStatus').value;window.LMSStore.write('training-programs',programs);header();render(document.querySelector('[data-program-tab].active').dataset.programTab);toast('Đã cập nhật chương trình')});$('copyProgram').addEventListener('click',()=>{const copy={...program,id:Math.max(...programs.map(item=>Number(item.id)||0))+1,name:`${program.name} - Bản sao`,status:'Ngừng hoạt động',created:new Date().toLocaleString('vi-VN')};programs.unshift(copy);window.LMSStore.write('training-programs',programs);toast(`Đã tạo bản sao chương trình #${copy.id}`)});header();render();
})();
